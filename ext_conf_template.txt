# cat=Backend; type=boolean; label=Direct Mail plain text rendering : Add the Diver-type to the direct_mail plaintext rendering
#enable.xclass.directMailPlainText = 1

# cat=Maintenance; type=string; label=Query Log Information Types : Log the following information... query,time,trail,error,explain,indices
#enable.xclass.queryLogInfoTypes = query,time

# cat=Frontend; type=boolean; label=Frontend Remove JS : Remove annoying inline JS scripts
#enable.xclass.feRemoveJs = 1

# cat=Frontend; type=boolean; label=Newsletter : Add a newsletter subscription to the Plugin selection
#enable.functions.newsletter = 1

# cat=Frontend; type=boolean; label=Language Detection : Enable the Language detection Plugin for the Website
#enable.functions.languageDetection = 1

# cat=Frontend; type=boolean; label=Static HTML Plugin : Enable the static HTML Plugin for Development of frontend extension templates
#enable.functions.staticHtmlPlugin = 0

# cat=Frontend; type=boolean; label=Robots Meta Tag control : Set options for the robots meta tag. The tag will be rendered by a TypoScriptFrontend Hook
#enable.functions.metaRobots = 0

# cat=Frontend; type=boolean; label=TS Object : Enable the typoscript object plugin
#enable.functions.tsObject = 0

# cat=Frontend; type=boolean; label=Address Finder : Enable the AddressFinder Plugin
#enable.functions.addressFinder = 0

# cat=Frontend; type=boolean; label=Lightbox : Enable lightbox.
#enable.functions.lightbox = 0

# cat=Backend; type=boolean; label=Bugfix Extbase Repository Relation : Bug information http://forge.typo3.org/issues/35851
#enable.bugfix.extbaseRepositoryRelation = 0

# cat=Backend; type=boolean; label=HDNET branding : Enable the branding of HDNET stuff in the backend
#enable.functions.hdnetBranding = 1

# cat=VCS; type=string; label=Username : --username PAR
#enable.vcs.username =

# cat=VCS; type=string; label=Password : --password PAR
#enable.vcs.password =
